#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry point
 * Description: 'print lower case alphabets'
 * Return: Always 0
 **/

int main(void)
{
	char c = 'a';

	for (c = 'a'; c <= 'z'; c++)


	putchar(c);
	putchar('\n');

	return (0);
}

